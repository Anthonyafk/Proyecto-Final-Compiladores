variable ENTERO[] numeros = [10, 20, 30];
variable ENTERO x = 10;
numeros[2] = 99;
agregar(numeros, 40);

variable ENTERO y = x;

imprimir(numeros[2]);

imprimir(numeros[0]);

eliminar(numeros, 1);

agregar(numeros, 50);
